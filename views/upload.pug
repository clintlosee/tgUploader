extends layout

include mixins/_uploadForm

block header

block content
  .inner
    h2= title
    +uploadForm()

    if uploads.length > 0
      .upload
        strong #{heading}
        each upload in uploads
          p URL: 👉 #{url}/uploads#{!upload.tag ? '' : '/' + upload.tag}/#{upload.file} 👈
          p: a(href=`${url}/uploads${!upload.tag ? '' : '/' + upload.tag}/${upload.file}`) #{upload.file}
          hr
    else
      .upload
        p No files uploaded

  a(href='/delete' class="delete-hidden") Delete Files